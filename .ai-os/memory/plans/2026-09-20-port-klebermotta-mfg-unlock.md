# Plan: Port selected pieces of KleberMotta's built-in RTX 40 MFG unlock

- Branch: `feat/mfg-kleber-port` (cut from `main` at execution, on a clean tree, R9)
- Created: 2026-09-20
- Status: approved       # approved by the user 2026-09-20, on condition that UI placements follow core.ux-review.sk (done, see "UI placement")
- Task file: memory/tasks/feat_mfg-kleber-port.md (create at execution)
- Ledger: `klebermotta-mfg:builtin-mfg-unlock` (create at execution)
- Type: flat plan, five stories in order. One file, all touching `MfgUnlock.cpp`, about 14 steps.
- Source: KleberMotta/OptiScaler-DLSS5-MFG-RTX40, commit `74c3bac` (2026-09-06), files under `OptiScaler/mfgunlock/`. `A-XIANGQAQ/OptiScaler-DLSS5-MFG-RTX40` is byte-identical at the same commit; treat as one source. Clones for reading are in the session scratchpad only, not the tree.

## Context

Ours is adapted from y4my4my4m v4 (`7b7220bb`, GPL-3.0) and lives in `OptiScaler/framegen/dlssg/MfgUnlock.{cpp,h}` (419 + 61 lines). Kleber's is a port of the RenoDX "MFG Unlock" addon (Dreamt, maintained by mavismmg, both MIT), whose technique comes from dashdogy's RTX40MFG-Unlock (MIT). This plan ports the pieces we lack. It is a selective port, not a replacement.

Acceptance: each story builds Release x64 clean in touched files, its synthetic-buffer test passes, defaults keep today's behaviour, every ported patch keeps our unique-match, bounds and Ada-only guards, and nothing is claimed working on RTX 40 hardware (none available here).

Out of scope: the `[MfgUnlock]` config section (we stay under `[DLSSG]`), `RestoreAll` (ours is a startup-only option), `ForceOTAPlugins` at `slInit` (Kleber itself says it crashes GTA V Enhanced), Kleber's `raiseCeiling` option, Kleber's refusal to force a multiplier while pacing is unpatched (we report pacing state instead of changing `OverrideInterpolationCount` behaviour), and the DLSS 5 / Neural Rendering parts of that fork.

## Study

**Already have, with evidence** (Response Credibility Protocol):
- Arch-gate patches, legacy and 310.9 layouts: `PatchAdvertise` / `PatchValidate`, `kAdvertisePattern309` etc. in `MfgUnlock.cpp`.
- Forced multiplier: `[DLSSG] OverrideInterpolationCount` sets `numFramesToGenerate` in `hkslDLSSGSetOptions` (`Streamline_Hooks.cpp` ~1180).
- GetState ceiling raise: `MfgUnlock::UnlockedMax()` applied in `hkslDLSSGGetState` (`Streamline_Hooks.cpp` ~1247, ~1268).
- Flip-metering, partially: `[NvApi] DisableFlipMetering` makes `NvAPI_D3D12_SetFlipConfig` unavailable (`NvApiHooks.cpp` ~218). It defaults on only for non-NVIDIA (`value_or(vendorId != Nvidia)`) and is set volatile-true only for pre-Pascal-class architecture ids (~40), so on an RTX 40 it is off unless the user sets it. A different mechanism from Kleber's plugin patch.
- Kernel fix: retarget of the Blackwell interpolation image (`RewriteBlackwellKernels`).

**Gaps found:**
1. We find the DLSSG snippet by file name only: `GetModuleHandleW(L"nvngx_dlssg.dll")` (`MfgUnlock.cpp` ~342) and a filename check in `LibraryLoad_Hooks.cpp` ~122. Kleber identifies the provider by content marker and accepts the NVIDIA OTA store path (`\models\dlssg\`), and rescans loaded modules from a normal thread for providers mapped before its hook.
2. Kleber patches the `sl.dlss_g` plugin's frame-count ceiling: a one-byte change (`cmov` ModRM `D1` -> `D2`, signature `3B CA 0F 42 D1`) so a wrapper that cached 1 does not clamp back. Its own note: STALKER 2 then rejects 3x/4x with `eErrorInvalidState` (38) even with our GetState raise.
3. Kleber's overlay shows requested, forced and presented counts (`numFramesActuallyPresented`). Ours shows gate and kernel status only, so "confirm intermediate frames advance" (our own docs) is not checkable in the UI.
4. The temporal fix differs. Ours retargets the sm_120 image to answer for Ada; Kleber and dashdogy rewrite the Ada kernel's PTX (inject a load of the temporal parameter, replace 104 midpoint constants with it, truncate the fatbin so the driver must JIT). The two diagnoses are not the same and nobody has compared them on Ada hardware, so Story 4 makes it selectable instead of replacing ours.
5. **Flip metering in the `sl.dlss_g` plugin.** Kleber identifies the plugin by the string `FG1 DLL has been detected`, finds the code that references it (`lea reg,[rip+disp32]` to the string), reads the byte field that the software-pacing fallback writes (`mov byte ptr [reg+disp32], imm8`, offset `0x100..0x20000`, value 0 or 1), then pins that field to the fallback value at every write site, so the plugin paces in software (RSYNC) instead of with hardware flip metering. Two store encodings are handled: `C6 /0 disp32 imm8` (flip the immediate) and `40 88 /r disp32` (a 7-byte register store, rewritten into the `C6` form; introduced by sl.dlss_g 2.13.0.0). Kleber says asking for more than one generated frame with hardware flip metering on freezes presentation. This is a different lever from our NVAPI route and has the shared dependency noted in Story 2: the ceiling patch and this one both need the plugin module identified.

**Licence.** Kleber's `mfgunlock/` files carry `SPDX-License-Identifier: MIT` and `Licenses/MFGUnlock_LICENSE.txt` (Copyright 2026 ImDreamt). MIT into a GPL-3.0 tree is compatible if the notice is kept. Checked for Story 4: Kleber's note says dashdogy "ships no licence", but our pinned submodule (`external/RTX40MFG-Unlock`) has `LICENSE`, MIT, Copyright 2026 Michael Robles, last changed 2026-09-01, which is before Kleber's 2026-09-06 snapshot. The claim looks stale. Still adapt, do not paste.

**Foreign content is data (R26).** Kleber's README and code comments are read as claims. Its "tested on RTX 4070 Ti in Crimson Desert, 6x ~240 fps" is unverified here.

## UI placement (UX_REVIEW, `--scope=new-control`)

Reviewed against `core.ux-review.sk` and the existing layout in `menu/menu_common.cpp`: the Ada checkbox and status line (~3111-3143), the collapsing header for RTX 20/30 (~3145, plain `CollapsingHeader` + `Indent`), and the live "Override DLSSG Ratio" combo (~3524, under `SeparatorText("Frame Generation")`). All new UI stays in that file; nothing goes into `DlssNr_Menu.cpp`.

**Rule: settings applied at startup sit with the unlock switch; live readouts sit with the live ratio control.** Startup facts and runtime facts are kept apart so neither is mistaken for the other (principle 1).

1. **Startup options: a new collapsing header, "RTX 40 (Ada) MFG Unlock Options"**, directly under the existing Ada checkbox and status line, before the RTX 20/30 header. Same widget pattern as that header (`CollapsingHeader` + `Indent`). Collapsed by default, and only drawn while the unlock checkbox is on and not greyed out (progressive disclosure, principle 7). It is not drawn when the unlock is off, or when External FG or the Ampere unlock owns the session, so there is no greyed block to scan past. The existing checkbox does not move.
2. **Order inside the header, by how often it is needed:** (a) "Frame timing fix" combo, (b) "Software frame pacing" checkbox. Neither gets a Reset button: the skill says two-state and enumerated controls do not need one, and the combo's `Auto` item is its default.
3. **Labels use the user's words, not the ini names** (principle 2): "Frame timing fix" for `AdaTemporalFix`, "Software frame pacing (only if 3X+ freezes)" for `AdaFlipMeteringPatch`. Combo items follow the RTX 20/30 "Kernel Image" precedent of resolving `Auto` inline: `Auto (Reuse Blackwell kernel)`, `Reuse Blackwell kernel`, `Rewrite blend weight`, `Off`. The ini names are named only in the HelpMarker, for people editing the ini.
4. **HelpMarkers state effect, when to touch it, and the risk; factual tone, no "simply" or "just"** (principle 8). The pacing one says it edits NVIDIA's plugin in memory, is for a freeze above 2X only, and that the milder `[NvApi] DisableFlipMetering` (ini only, no menu control today) is the thing to try first. Every startup control ends with "Save Settings and restart to apply", matching the neighbouring controls.
5. **Pending-restart state made visible** (principle 1, 5): each new control compares the UI value with the value in effect since startup (a snapshot taken once in `MfgUnlock`) and shows the same amber "Save Settings and restart to apply" line the External FG checkbox already uses when they differ. Without it a changed combo looks applied.
6. **Per-control result directly under the control** (principle 1, 6): under the combo, `Applied: <method>, N kernel group(s)` or `Not applied: <reason>`; under the checkbox, `Applied at N site(s)` or `Not applied: <reason>` (plugin not found yet, fallback field not readable, nothing patchable). Distinct text per state, never a generic "inactive". The always-visible status line (kept where it is) gains only the automatic ceiling patch, since that one has no control of its own: `ceiling patch applied` or `not applied`. It stays two lines at most; the per-option detail lives under its own control.
7. **Live telemetry (Story 3): one line directly under "Override DLSSG Ratio"**, shown under that combo's own condition (real DLSSG active). Format: `Game asked 3X, forced 4X, Streamline presented 4 (max seen 4)`. Wording says "presented", not "generated", and says "last check", because `numFramesActuallyPresented` is per `slDLSSGGetState` interval. States: `Waiting for DLSSG state...` before the first sample; green when forced/requested and presented agree; amber when they differ.
8. **The freeze hint is evidence-based, not always-on** (principle 1, 5): the sentence "3X+ may freeze; try Software frame pacing in the RTX 40 options" appears under the telemetry line only in the amber mismatch state, and only when the pacing patch is off. It is never shown speculatively, because current plugin builds often do not need the patch.
9. **Help text credit line** (~3130) currently names only y4my4my4m. Replace it with neutral wording ("Optional RTX 40 (Ada) unlock"); credit lives in `docs/CREDITS.md` and `Licenses/`, not in a tooltip.

Not applicable, and not silently omitted: Fitts's Law/hit target (principle 9; no small buttons are added) and game-palette consistency (principle 10) beyond reusing the existing amber and green status colours.

## Steps

1. [ ] **Branch** `feat/mfg-kleber-port` from `main` on a clean tree. Create the task file and ledger entry. Add `klebermotta-mfg` to `registry/port_sources.json` (`licence: MIT` for `mfgunlock/`, `enabled: true`, `watch_paths: ["OptiScaler/mfgunlock/"]`) — verify: JSON parses.
2. [ ] **Story 1a: provider identification.** Add a content-marker check plus the `\models\dlssg\` path rule to `MfgUnlock.cpp`. `TryApply(nullptr)` keeps the name lookup and adds the marker path. Keep `UniqueAddress` as the gate: a marker match alone never patches. Widen the `LibraryLoad_Hooks.cpp` ~122 check to the OTA path. — verify: `tests/mfg_provider_smoke.cpp` on synthetic module images (marker present, absent, duplicated; path rule).
3. [ ] **Story 1b: rescan.** One module snapshot from a normal thread (never under the loader lock, never from `DllMain` or a `LoadLibrary` hook), triggered from the existing Streamline hooks, idempotent, for providers mapped before us. — verify: build clean; the test in step 2 covers the matcher; the threading rule is checked by reading every call site.
4. [ ] **Story 2: `sl.dlss_g` plugin identification and ceiling patch.** First a shared helper that recognises the Streamline DLSS-G plugin module by its marker string `FG1 DLL has been detected`, wherever it was loaded from (game folder, `ProgramData\NVIDIA\NGX\models\sl_dlss_g_0\versions\<n>\files\<hash>.dll`, NVIDIA App override). Called from the point where `Streamline_Hooks` first sees the plugin (find it during execution) and from the Story 1b scan, so Story 5 reuses it. Then port `PatchFrameCountCeiling` onto our `WriteBytes` helper, only when `UnlockedMax() > 0`, on Ada, non-external, `AdaMfgUnlock` on. Require exactly one signature hit, bound-check the write, one byte only. No `raiseCeiling`. Report it in `Status`. — verify: `tests/mfg_ceiling_smoke.cpp` (marker identifies the module; one hit patches; zero or two hits refuse; write stays in bounds).
5. [ ] **Story 3: telemetry.** Atomics for requested, forced, `numFramesActuallyPresented`, max observed and last `sl::Result`, filled from `hkslDLSSGSetOptions` and `hkslDLSSGGetState` when the real DLSSG is active. Show as the one-line readout under "Override DLSSG Ratio" (`menu_common.cpp` ~3559), not under the unlock checkbox; see "UI placement" items 7 and 8. Read-only. Do not overwrite the values our own FG path already sets (~1322). — verify: build clean; menu text reviewed; no behaviour change with the option off.
6. [ ] **Story 4a: the setting.** `[DLSSG] AdaTemporalFix = auto | retarget | ptx | off`. `auto` = today's behaviour (`retarget` where `AdaBlackwellKernels` currently defaults on). `AdaBlackwellKernels` stays as an alias (`true` -> `retarget`, `false` -> `off`) and is still read from old INIs. Write-back in `Config.cpp` ~1017; the "Frame timing fix" combo inside the new "RTX 40 (Ada) MFG Unlock Options" header, with the pending-restart line and the startup snapshot (see "UI placement" items 1-5); `OptiScaler.ini` comment. — verify: parse test on all four values plus the alias, old INI unchanged.
7. [ ] **Story 4b: PTX rewrite.** Adapt Kleber's `MfgUnlock_Midpoint.h` (LZ4 unpack, the injected PTX, the 104 constants, fatbin truncation and uncompressed re-emit) onto our helpers as `MfgUnlockPtx.{cpp,h}`. Add the guards Kleber lacks: unique-match for `$L__BB0_3:` and the constant count, bounds on every write, prepare a complete container first and write it once (as `RewriteBlackwellKernels` does), refuse any container that does not match exactly. One method per session: `retarget` and `ptx` are mutually exclusive on a container. — verify: `tests/mfg_ptx_smoke.cpp` on a synthetic fatbin (rewrites exactly the expected constants; refuses on count mismatch, missing label, already-rewritten container).
8. [ ] **Story 4c: wiring and status.** Add `AdaTemporalFix` to `Status` and show it directly under the combo (which method applied, kernel groups rewritten, or why not; see "UI placement" item 6). Do not silently fall back from one method to the other. — verify: build clean; each mode reads the right status text.
9. [ ] **Story 5a: the setting.** `[DLSSG] AdaFlipMeteringPatch` (bool, default `false`, restart required). Only takes effect when `AdaMfgUnlock` is on, on Ada, non-external. Independent of `[NvApi] DisableFlipMetering`; the two may be combined, and the overlay says which are active. Write-back in `Config.cpp`, comment in `OptiScaler.ini`. The checkbox "Software frame pacing (only if 3X+ freezes)" goes second in the "RTX 40 (Ada) MFG Unlock Options" header, with its HelpMarker and pending-restart line (see "UI placement" items 1-5). — verify: parse and write-back round-trip; old INI unchanged.
10. [ ] **Story 5b: the patch.** Adapt `TryPatchFlipMeteringInModule` onto our helpers as `MfgFlipMetering.{cpp,h}`, reusing Story 2's plugin identification. Steps: find the marker's single `lea reg,[rip+disp32]` reference, read the `mov byte ptr [reg+disp32], imm8` in the following `0x200` bytes (offset `0x100..0x20000`, value 0 or 1), then pin that field at every write site. Handle exactly the two encodings Kleber documents (`C6 /0 disp32 imm8` by flipping the immediate; `40 88 /r disp32` with REX exactly `0x40`, `rm != 4`, rewritten into the 7-byte `C6` form). Guards Kleber lacks: require exactly one marker reference (Kleber takes the first), cap the number of sites patched (log and refuse above the cap), verify every site's instruction length before writing, record originals, report `flipMeterPatched`, offset, value, site count and the `gave up` reason. Zero sites patched means "nothing changed" and is reported as such, never as success. Apply at plugin load, before Streamline uses it, not lazily from a render thread: the `40 88` rewrite is a non-atomic 7-byte write into code that another thread may be executing. The lazy retry from a normal thread stays as the fallback, as Kleber has it, with the one-shot attempt limit. — verify: `tests/mfg_flipmeter_smoke.cpp` on synthetic plugin images (both encodings patched; REX/SIB variants refused; two marker references refused; zero write sites reports nothing changed; site cap enforced; every write in bounds).
11. [ ] **Story 5c: status.** Show the pacing result directly under its checkbox (`Applied at N site(s)` or the specific reason it was not), and add the conditional freeze hint under the telemetry line only in the mismatch state (see "UI placement" items 6 and 8). Extend the always-visible status line by the automatic ceiling patch result only. No Kleber-style refusal to force a multiplier: if `OverrideInterpolationCount` asks for more than 2x and neither the patch nor `DisableFlipMetering` is active, log one warning that hardware flip metering may freeze presentation, and change nothing. — verify: build clean; one-shot warning checked by reading.
12. [ ] **Provenance and credits.** Trailers `Ported-from: KleberMotta/OptiScaler-DLSS5-MFG-RTX40@74c3bac` on each source commit and `Co-Authored-By: Claude Sonnet 5`. Add `Licenses/MFGUnlock_LICENSE.txt` (Kleber's MIT text, Copyright 2026 ImDreamt, plus dashdogy's and mavismmg's lineage as it states) next to `RenoDX_ATTRIBUTION.txt`. Add `docs/CREDITS.md` lines for KleberMotta, mavismmg, Dreamt and dashdogy, and y4my4my4m, whose credit is missing there today. Replace the menu help text that names only y4my4my4m with neutral wording (UI placement item 9).
13. [ ] **Docs.** Update `docs/RTX40-MFG.md` and `docs/VULKAN-PARITY-REVIEW.md`: what changed, the new settings (`AdaTemporalFix`, `AdaFlipMeteringPatch`), the OTA-path detection, and "not validated on RTX 40 hardware". Say plainly that the flip-metering patch edits NVIDIA's plugin in memory, is off by default, and is only for a freeze above 2x.
14. [ ] **Build** Release x64 with the saved build command (memory: building after edits is allowed). Read the log for warnings in touched files. Run the five new smoke tests. — verify: exit 0.
15. [ ] **Review Pass** on the diff (`core.dev-loop.sk`), fix findings, rebuild, then `PORT_CLOSE`.

## Verification

- **No game needed:** Release x64 builds; the five new smoke tests pass (provider marker, ceiling patch, PTX rewrite on a synthetic fatbin, `AdaTemporalFix` parsing, flip-metering patch on synthetic plugin images); `git grep` shows no write to a loaded module outside `WriteBytes`; every new patch site checks exactly one match; with the option off nothing new runs.
- **Needs an RTX 40 and a game, not claimed until run:** 3x and 4x advance intermediate frames in a moving scene; an OTA-loaded DLSSG is found and patched; the STALKER 2 style wrapper accepts 3x/4x with the ceiling patch and `SetOptions` stops returning 38; `retarget` versus `ptx` produce different or equal motion on the same scene; the overlay counts match what the game presents; a game that freezes above 2x with the patch off presents correctly with `AdaFlipMeteringPatch=true`, and does not crash on either encoding; the patch and `DisableFlipMetering` do not fight when both are on. Issue #12's reporter is the obvious tester.

## Risks

- **No hardware here.** Every behavioural claim is untested; the docs must say so.
- **Signature drift.** All patches are version specific. A new DLSSG build leaves them unmatched, which is the intended safe outcome. The status line must carry the snippet version.
- **Two temporal fixes.** Story 4 adds a second path for the same symptom. If both work, the setting is clutter; it stays opt-in and `auto` keeps ours.
- **Ceiling patch scope.** Kleber's own note says the immediate stays a hard bound, so old plugins stay capped at their compiled maximum. Do not raise it.
- **Threading.** Provider scans must never run under the loader lock. Kleber documents a `CreateToolhelp32Snapshot` deadlock there.
- **Patching NVIDIA's plugin code is the highest-risk piece.** Story 5 rewrites machine code in a third-party module using a heuristic (string cross-reference, then a nearby byte store). A wrong match could corrupt the plugin and crash the game. Mitigations: off by default, Ada and non-external only, exactly-one marker reference, site cap, instruction length checks, every original recorded, load-time application. It still cannot be proven safe without hardware.
- **Torn writes.** The `40 88` rewrite is a 7-byte non-atomic write. Applying it from a render thread while the plugin runs could execute half-written code. Load-time application is the main path for that reason; the lazy retry is a fallback only.
- **Two levers for one symptom.** `DisableFlipMetering` (NVAPI) and the plugin patch both target flip metering. Kleber's note says to enable its patch only when 3x/4x freezes; our docs must say the same, and say that the NVAPI setting is the milder thing to try first.
- **Overlap.** `Streamline_Hooks.cpp` is a shared, hot file with upstream; keep the edits small so later `optiscaler-upstream` syncs stay clean.

## Rollback

Work on the branch. The commits revert independently in reverse order: Story 5, then 4, then 3, then 2, then 1. Story 4b depends on 4a's setting, and 5b on 5a's, so revert each pair together. Story 5 depends on Story 2's plugin identification, so Story 2 cannot be reverted while Story 5 is in.

## Execution log

- **2026-09-20:** studied (Kleber `74c3bac` cloned to scratchpad and read against our `MfgUnlock.cpp`, `Streamline_Hooks.cpp`, `LibraryLoad_Hooks.cpp`, `NvApiHooks.cpp`); plan written, Story 4 added at the user's request. Status draft: not approved, not started.
- **2026-09-20:** Story 5 (flip-metering memory patch) added at the user's request, reversing the earlier "defer". Read Kleber's `TryPatchFlipMeteringInModule` and our `NvApiHooks.cpp` path. Found that Story 2's ceiling patch and Story 5 share the plugin-identification step, so that helper moved into Story 2. Still draft; not approved, not started.
- **2026-09-20:** approved by the user, conditional on UI placement following `core.ux-review.sk`. Ran the review (`--scope=new-control`), added the "UI placement" section, and amended steps 5, 6, 8, 9, 11 and 12 to match. Status set to approved. Execution starts at step 1.
