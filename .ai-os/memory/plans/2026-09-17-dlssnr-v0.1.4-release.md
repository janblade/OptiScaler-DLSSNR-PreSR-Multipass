# Plan: Ship fix/dlssnr-nr-menu-ux as v0.1.4 (menu UX polish + Optimized Defaults preset)
- Branch: fix/dlssnr-nr-menu-ux -> main
- Created: 2026-09-17
- Status: abandoned (steps 1-4 done: merged as PR #13 into main; steps 5+ cancelled by user
  request before a tag or GitHub Release existed for v0.1.4 -- nothing to clean up beyond
  cancelling the in-progress CI run)
- Task file: memory/tasks/fix_dlssnr-nr-menu-ux.md

## Context
User asked to "prepare for release." Investigation found: (1) v0.1.3 (PR #12) is already a
published GitHub Release with a packaged zip -- no catch-up needed, an earlier claim to the
contrary was based on local git tags only and was wrong (corrected after checking `gh release
list` directly). (2) Releases here are built via a `workflow_dispatch` GitHub Actions job
(`.github/workflows/package_release.yml`) that runs `package_release.ps1` on a hosted runner
and uploads the zip as a build artifact; a human/agent then publishes that artifact as a
GitHub Release with `gh release create`. (3) User confirmed: merge the current branch's two
already-completed, build-verified plans (menu-UX-polish 8/8, optimized-defaults 4/4) into
main myself now, then run the release pipeline through to a published v0.1.4 GitHub Release.

Both source commits are already build-verified (Debug+Release x64, 0 errors) and reviewed
(self-review + independent-reviewer Review Pass, both clean). Bundled as one commit rather
than two -- the edits from both plans interleave in the same function/file from the same
session, and precise hunk-level separation risked more than it was worth for two small,
already-reviewed, already-related changes going into one PR regardless.

## Steps
1. [ ] Commit `OptiScaler/dlssnr/DlssNr_Menu.cpp` plus the two plan files and the task file
   update on `fix/dlssnr-nr-menu-ux`. -- verify: `git status` clean after commit.
2. [ ] Push the branch to origin. -- verify: `git log origin/fix/dlssnr-nr-menu-ux` shows the
   new commit.
3. [ ] Open PR #13 via `gh pr create`, titled to cover both pieces. -- verify: PR URL returned.
4. [ ] Merge the PR (`gh pr merge --merge`, matching this repo's existing merge-commit
   convention, not squash) -- per explicit user go-ahead, no external review pause. --
   verify: `gh pr view 13` shows MERGED; `git fetch && git log origin/main -3` shows the
   merge commit.
5. [ ] Trigger `package_release.yml` via `gh workflow run` with `version=v0.1.4-optimized-defaults`
   (slug names the more user-visible of the two shipped changes, matching this repo's
   single-feature-slug tag convention). -- verify: workflow run reaches `completed`/`success`.
6. [ ] Download the resulting build artifact (`gh run download`). -- verify: zip present
   locally.
7. [ ] Create annotated tag `v0.1.4-optimized-defaults` on the PR's merge commit, push it.
   -- verify: `gh api repos/:owner/:repo/git/refs/tags/...` or `git ls-remote --tags origin`
   shows it.
8. [ ] Publish the GitHub Release from that tag with the downloaded zip attached, notes
   modeled on v0.1.3's release body (what changed, PR link, no default-settings-changed
   note where true). -- verify: `gh release view v0.1.4-optimized-defaults` shows the asset.
9. [ ] Update task file / decisions.jsonl to record the merge and release.
